Como compilar:

O programa poder ser compilador atravez do eclipse. Para isso basta selecionar o package main, e dentro do package selecionar a classe Main.java.
Com a classe selecionada basta clicar em Run Main e o programa será compilado e execultado.

Obs: O programa já esta compilado e exportado como arquivo .jar, podendo ser execultado diretamente sem compilação.